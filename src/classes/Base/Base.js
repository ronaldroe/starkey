import Logger from "./Logger.js";

/**
 * @classdesc Base class. Currently used for the User classes, but may just be removed
 *
 * @name Base
 * @class
 * 
 * @extends Logger
 */
export default class Base extends Logger {
  constructor() {
    super();
  }
};
